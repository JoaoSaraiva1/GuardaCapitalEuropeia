@model IEnumerable<GuardaCapitalEuropeia.Models.Mesa>

@{
    ViewData["Title"] = "Index";
}
<style>

    body {
        background-color: black;
    }

    table {
        background-color: #353636;
    }

    th {
        color: white;
    }

    p {
        color: white;
        margin-left: 30px;
    }

    tr {
        color: white;
    }

    a {
        color: lightslategray
    }
</style>


<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Numero)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Restaurante)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Lugares)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Numero)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Restaurante)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Lugares)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.MesaId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.MesaId">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.MesaId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
