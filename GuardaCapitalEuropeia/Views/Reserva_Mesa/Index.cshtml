@model IEnumerable<GuardaCapitalEuropeia.Models.Reserva_Mesa>

@{
    ViewData["Title"] = "Reservas";
}
<style>

    body {
        background-color: black;
    }

    table {
        background-color: #353636;
    }

    th {
        color: white;
    }

    p {
        color: white;
        margin-left: 30px;
    }

    tr {
        color: white;
    }

    a {
        color: lightslategray
    }
</style>


<h1>Reservas</h1>

<p>
    <a asp-action="Create">Criar nova Reserva</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Numero)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Data_Reserva)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hora_Reserva)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pessoas)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Numero)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Data_Reserva)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Hora_Reserva)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Pessoas)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Reserva_MesaId">Editar</a> |
                    <a asp-action="Details" asp-route-id="@item.Reserva_MesaId">Detalhes</a> |
                    <a asp-action="Delete" asp-route-id="@item.Reserva_MesaId">Apagar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
